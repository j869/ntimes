<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <header>
        <h1><%= title %></h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <% if (!user) { %>
                    <li><a href="/login">Login</a></li>
                <% } else { %>
                    <li>Welcome, <%= user.username %> (<a href="/logout">Logout</a>)</li>
                <% } %>
            </ul>
        </nav>
    </header>
    <main>
