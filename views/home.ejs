<%- include('partials/header') %>

<section class="col py-3">
  <div class="container pt-2 m-0 bglight">
    <i class="fas fa-key fa-4x"></i>
      <h1 class="display-3">numbat</h1>
      <p class="lead">Timekeeping done naturally</p>
      <hr>
      <% if (user) { %>
        <p>Hello, <%= user.username %>. Welcome to Numbat Timekeeping.</p>
        <a class="btn btn-light btn-lg" href="/logout" role="button">Logout</a>
        <% } else { %>
          <a class="btn btn-light btn-lg" href="/register" role="button">Register</a>
          <a class="btn btn-dark btn-lg" href="/login" role="button">Login</a>
          <% } %>
    </div>
</section>


  <%- include('partials/footer') %>